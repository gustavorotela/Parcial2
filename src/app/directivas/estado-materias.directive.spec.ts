import { EstadoMateriasDirective } from './estado-materias.directive';

describe('EstadoMateriasDirective', () => {
  it('should create an instance', () => {
    const directive = new EstadoMateriasDirective();
    expect(directive).toBeTruthy();
  });
});
