import { CupoAlumnosDirective } from './cupo-alumnos.directive';

describe('CupoAlumnosDirective', () => {
  it('should create an instance', () => {
    const directive = new CupoAlumnosDirective();
    expect(directive).toBeTruthy();
  });
});
