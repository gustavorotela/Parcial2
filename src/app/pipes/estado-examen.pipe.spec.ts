import { EstadoExamenPipe } from './estado-examen.pipe';

describe('EstadoExamenPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoExamenPipe();
    expect(pipe).toBeTruthy();
  });
});
