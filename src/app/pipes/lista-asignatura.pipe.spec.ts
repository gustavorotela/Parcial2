import { ListaAsignaturaPipe } from './lista-asignatura.pipe';

describe('ListaAsignaturaPipe', () => {
  it('create an instance', () => {
    const pipe = new ListaAsignaturaPipe();
    expect(pipe).toBeTruthy();
  });
});
